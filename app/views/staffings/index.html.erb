人員配置表  
  <div class='calendar'>
    <div class='members'>
      <% @members.each do |member| %>
        <div class='member'><%= member.name %></div>
      <% end %>
    </div>
    <% @week_days.each do |day| %>
      <div class='item'>
        <div class='date'>
          <%= day[:month] %>/<%= day[:date] %><%= day[:wday] %>
        </div>
        <% Member.count.times do |i| %>
          <div class='position'>
            <%= day[:staffing][i] %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>

  <div class='buttons'>
    <% unless Staffing.exists? %>
      <%= link_to "入力画面へ", new_staffing_path, class: "button" %>
    <% else %>
      <%= link_to "削除", destroy_all_staffings_path, method: :delete, class: "button" %>
    <% end %>
  </div>

  <% @pick_uniq.count.times do |i| %>
    <div class="uniq">
      「<%= @pick_uniq[i][:member] %>」は「<%= @pick_uniq[i][:position] %>」に4回以上配置されています。
    </div>
  <% end %>